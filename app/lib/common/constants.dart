import 'package:flutter/animation.dart';

const kAppName = 'pub.dev explorer';

// Database
const kDatabaseName = 'app.sqlite';

// Packages
const kPubUrl = 'https://pub.dev/';
const kPackageCacheDuration = Duration(days: 4);

// Bookmarks
const kBookmarksFetchLimit = 50;

// UI
const kContentMaxWidth = 900.0;
const kSlideDuration = Duration(milliseconds: 300);
const kSlideCurve = Curves.ease;
